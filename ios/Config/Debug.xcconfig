#include "Base.xcconfig"

// xcconfig treats // as a comment. Escape the // in URLs with $().
DISPLAY_NAME = Finance AI (Stage)

API_BASE_URL = http:/$()/127.0.0.1:8000
APNS_SANDBOX = YES

SWIFT_ACTIVE_COMPILATION_CONDITIONS = DEBUG
